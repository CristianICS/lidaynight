% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_stats.R
\name{reshape_statistics_long}
\alias{reshape_statistics_long}
\title{Reshape statistics from wide to long format}
\usage{
reshape_statistics_long(stats_wide)
}
\arguments{
\item{stats_wide}{A data frame or tibble containing statistic columns and the
metadata columns \code{time} and \code{height}.}
}
\value{
A tibble in long format with the columns:
\describe{
\item{time}{Acquisition time.}
\item{height}{Flight height.}
\item{statistic}{Character. Name of the statistic.}
\item{value}{Numeric. Statistic value.}
}
}
\description{
Converts a wide-format statistics table into long format, with one row per
statistic and acquisition condition.
}
\details{
All columns except \code{time} and \code{height} are treated as statistic columns and
are gathered into the \code{statistic} and \code{value} columns.
}
\examples{
\dontrun{
stats_wide <- load_all_statistics("stats", "roi_stats")
stats_long <- reshape_statistics_long(stats_wide)
}
}
