% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global_stats.R
\name{roi_global_metrics}
\alias{roi_global_metrics}
\title{Compute global LAS tile metrics}
\usage{
roi_global_metrics(
  Z,
  Intensity,
  ReturnNumber,
  NumberOfReturns,
  compute_duplicates = FALSE,
  X = NULL,
  Y = NULL
)
}
\arguments{
\item{Z}{Point height}

\item{Intensity}{Intensity value}

\item{ReturnNumber}{Represent the return number associated with the point.}

\item{NumberOfReturns}{Represent the amount of returns of the current point.}

\item{compute_duplicates}{If \code{TRUE}, compute duplicated points by check the
amount of data with equal X,Y,Z coordinates.}

\item{X}{Coordinate X of the point. Required only to compute duplicate points}

\item{Y}{Coordinate X of the point. Required only to compute duplicate points}
}
\value{
A named list with the computed metrics.
}
\description{
Compute descriptive acquisition/structure indicators. These
set of statistics could be computed without the need of a ground
classification. All of them are absolute values instead percentages because
these metrics are normally computed across different tiles. The percentages
are computed in a later stage.

The current function has been designed to include it inside
\code{\link[lidR:plot_metrics]{lidR::plot_metrics()}}. The function clips each ROI,
computes cloud metrics, and binds the metrics back to the ROI geometry.
It is documented as a wrapper around \code{\link[lidR:clip_roi]{lidR::clip_roi()}},
\code{\link[lidR:cloud_metrics]{lidR::cloud_metrics()}}, and \code{cbind}, and its func argument is a
formula/expression, not a \code{LAScluster} worker.
}
\details{
The derived metrics are (see \code{.global_stats_ptype} prototype):
\itemize{
\item n_pnts = Total number of points inside the tile file.
\item dup_pnts = The number of duplicated points.
\item intensity_avg = The mean intensity between all the valid points.
\item ret_\* = Number of points with 1, 2 and 3 returns. Currently
only 1-3 returns are computed.
\item Single-return points (pulses that generated exactly one return)
\item Points with more than one return
\item Points without Z data
\item Points with negative height
\item Thresholds for the 1st and 99th quantile of heights
\item Points from 0 to 1st quantile
\item Points from 99th to last quantile
}
}
\note{
The parameters of the function are the LAS point properties required to
compute the statistics.
}
