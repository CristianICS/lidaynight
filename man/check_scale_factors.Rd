% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_scale_factors.R
\name{check_scale_factors}
\alias{check_scale_factors}
\title{Check LAS/LAZ scale factor consistency}
\usage{
check_scale_factors(folder, pattern = "\\\\.(laz|las)$")
}
\arguments{
\item{folder}{Path to the folder containing the LAS/LAZ files.}

\item{pattern}{Regex used to match LAS/LAZ file names. Defaults to
\code{"\\\\.(laz|las)$"}.}
}
\value{
A named list with:
\itemize{
\item \code{info}: a \code{data.frame} with one row per file, listing its X/Y/Z
scale factors and offsets.
\item \code{all_same}: \code{TRUE} if every file shares the same X/Y/Z scale factor,
\code{FALSE} otherwise.
}
}
\description{
Reads the header of every LAS/LAZ file in a folder and checks whether
they all share the same X/Y/Z scale factor. Useful before deduplicating
points merged from multiple flightlines/tiles (e.g. via
\code{\link[lidR:filters]{lidR::filter_duplicates()}} or \code{duplicated()}), since files quantized
with different scale factors can decode the same physical point to
different floating-point coordinates, causing missed duplicates
(false negatives).
}
