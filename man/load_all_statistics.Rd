% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_stats.R
\name{load_all_statistics}
\alias{load_all_statistics}
\title{Load statistics from all acquisition folders}
\usage{
load_all_statistics(
  stats_dir,
  stat_prefix,
  height_levels = c(40, 75, 100),
  time_levels = c("day", "night")
)
}
\arguments{
\item{stats_dir}{Character scalar. Path to the main statistics directory.
This directory should contain one subfolder per acquisition condition,
named using the pattern \verb{<time>_<height>}, for example \code{"day_40"}.}

\item{stat_prefix}{The character corresponding with the required stat file
to find.}

\item{height_levels}{Numeric vector. Optional ordering of flight heights.
Defaults to \code{c(40, 75, 100)}.}

\item{time_levels}{Character vector. Optional ordering of acquisition times.
Defaults to \code{c("day", "night")}.}
}
\value{
A tibble in wide format. Each row corresponds to one acquisition
condition, with statistic columns plus:
\describe{
\item{time}{Factor. Acquisition time.}
\item{height}{Factor. Flight height.}
}
}
\description{
Reads statistics from all subfolders inside a main statistics directory and
combines them into a single wide-format table.
}
\details{
Each immediate subfolder of \code{stats_dir} is expected to contain exactly one
CSV file per \code{stat_prefix}. Folder names are used to infer acquisition
metadata.

This function does not reshape the statistic columns. Use
\code{\link[=reshape_statistics_long]{reshape_statistics_long()}} to convert the output to long format for
plotting.
}
