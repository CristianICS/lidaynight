% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog_compression.R
\name{compressLasFolder}
\alias{compressLasFolder}
\title{Compress the LAS files inside a folder}
\usage{
compressLasFolder(
  ctg_folder,
  overwrite = FALSE,
  create_lax = TRUE,
  remove_original = FALSE
)
}
\arguments{
\item{ctg_folder}{Path to a folder containing \code{.las} files.}

\item{overwrite}{If \code{TRUE}, overwrite existing \code{.laz} files.}

\item{remove_original}{If \code{TRUE}, remove the original \code{.las} file after the
corresponding \code{.laz} file has been succesfully written.}

\item{create_laxIf}{\code{TRUE}, create \code{.lax} spatial index files.
For newly written \code{.laz} files, this is handled by \code{\link[lidR:writeLAS]{lidR::writeLAS()}}.
For existing \code{.laz} files skipped because \code{overwrite = FALSE}, a missing
\code{.lax} file is created with \code{\link[rlas:writelax]{rlas::writelax()}}.}
}
\value{
Invisibly returns \code{TRUE}.
}
\description{
Converts all \code{.las} files in \code{ctg_folder} to \code{.laz} files using
\code{\link[lidR:readLAS]{lidR::readLAS()}} and \code{\link[lidR:writeLAS]{lidR::writeLAS()}}. By default, existing \code{.laz}
files are not overwritten.
}
\note{
This function reads each LAS file into R before writing it back as LAZ.
For very large LAS files, command-line tools such as \code{laszip} from
LAStools/rapidlasso may be more memory-efficient.
}
